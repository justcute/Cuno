<div class="navbar navbar-expand-lg sticky-top navbar-light bg-light">
      <div class="container">
        <a href="<%= root_path %>" class="navbar-brand">Cuno</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown" data-bs-toggle="dropdown" href="#">Movies<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <a class="dropdown-item" href="<%= movies_path%>">Popular</a>
                <a class="dropdown-item" href="#">Now Playing</a>
                <a class="dropdown-item" href="#">Top Rated</a>
                <% if user_signed_in? && (current_user.admin? || current_user.editor?) %>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#">New Movie</a>
                <% end %>
              </ul>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown" data-bs-toggle="dropdown" href="#">TV shows<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <a class="dropdown-item" href="#">Popular</a>
                <a class="dropdown-item" href="#">Now Playing</a>
                <a class="dropdown-item" href="#">Top Rated</a>
                <% if user_signed_in? && (current_user.admin? || current_user.editor?) %>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#">New TV Show</a>
                <% end %>
              </ul>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown" data-bs-toggle="dropdown" href="#">People<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <a class="dropdown-item" href="#">Popular</a>
                <% if user_signed_in? && (current_user.admin? || current_user.editor?) %>
                <div class="dropdown-divider"></div>
                <a class="dropdown-item" href="#">New Person</a>
                <% end %>
              </ul>
            </li>
            <% if user_signed_in? && current_user.admin? %>
            <li class="nav-item dropdown">
              <a class="nav-link dropdown" data-bs-toggle="dropdown" href="#">Admin rights<span class="caret"></span></a>
              <ul class="dropdown-menu">
                <a class="dropdown-item" href="#">Users</a>
                <a class="dropdown-item" href="#">Charts</a>
              </ul>
            </li>
            <% end %>
          </ul>
          <ul class="navbar-nav ms-md-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'nav-link' %>
              </li> 
              <li class="nav-item">
                <%= link_to 'Edit registration', edit_user_registration_path, class: 'nav-link' %>
              </li>        
            <% else %>
              <li class="nav-item">
                <%= link_to 'Login', new_user_session_path, class: 'nav-link' %>
              </li> 
              <li class="nav-item">
                <%= link_to 'Join', new_user_registration_path, class: 'nav-link' %>
              </li>
            <% end %>
          </ul>
        </div>
        <a class="nav-link dropdown" data-bs-toggle="dropdown" href="#"><svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
        <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
        </svg></a>
        <ul class="dropdown-menu  w-100">
          <form class="d-flex">
            <input class="form-control" type="text" placeholder="Search">
            <button class="btn btn-secondary" type="submit">Search</button>
          </form>
        </ul>
      </div>
    </div>


